[project]
name = "vak2csv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.11",
    "tqdm>=4.66",
]

[dependency-groups]
dev = [
    "pytest>=8",
]
